# Deployment Configuration
base_url: http://0.0.0.0:3000 


# Feature Flags
features:
  allow_uninvited_signups: true
  allow_multiple_organizations: false 
  verify_emails: false


auth:
  mode: "hybrid" # hybrid | local_only | sso_only

google_oauth:
  enabled: false

# oidc_providers:
#- name: okta
#  enabled: false
#  issuer: https://YOUR_OKTA_DOMAIN.okta.com/oauth2/default
#  client_id: ${MC_OKTA_CLIENT_ID}
#  client_secret: ${MC_OKTA_CLIENT_SECRET}
#  scopes: ["openid", "profile", "email"]
#  pkce: true
#  client_auth_method: basic
#  discovery: true
#  uid_claim: sub
 # Optional overrides:
 # redirect_path: /api/auth/okta/callback
 # extra_authorize_params: { prompt: login }
 # extra_token_params: {}

encryption_key: ${MC_ENCRYPTION_KEY}

intercom:
  enabled: false

telemetry:
  enabled: false

# license:
#    key: ${MC_LICENSE_KEY}